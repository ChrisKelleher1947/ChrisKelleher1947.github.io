Languages

Python 3.11
JavaScript (Node.js) for the WhatsApp bot

Frameworks & Libraries

Jupyter Notebook — interactive development environment
NumPy — numerical array operations
Pandas — reading protocol/TSV files, data manipulation
Matplotlib — visualising waveforms and spectrograms
Librosa — audio loading, feature extraction (Log-Mel, MFCC)
SoundFile — reading/writing WAV files
PyTorch — deep learning framework (installing now)
Torchaudio — audio processing utilities for PyTorch
HuggingFace Transformers — wav2vec2 pretrained model and fine-tuning
Accelerate — HuggingFace training optimisation

Models

facebook/wav2vec2-base — pretrained transformer for speech 
PANNs CNN14 — pretrained CNN on AudioSet

Datasets

ASVspoof 2019 Logical Access (LA) — primary training and evaluation dataset
Mozilla Common Voice 24.0 (English) — out-of-domain test set

Cloud Infrastructure

AWS SageMaker — model hosting and inference endpoint
AWS EC2 — hosting WhatsApp bot and FastAPI backend
AWS S3 — model storage
AWS IAM — access control

Tools

Anaconda — Python environment management
Git / GitHub — version control

